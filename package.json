{
  "name": "utratadochodu",
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i src/main.css -o tailwind.css --minify",
    "watch:css": "npx @tailwindcss/cli -i src/main.css -o tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0"
  }
}
